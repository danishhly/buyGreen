spring.application.name=buygreen

# Database Configuration
# ===============================
# Use environment variables with defaults for local development
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/buygreen_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:0598}

# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Razorpay Configuration
# ===============================
# Use environment variables - NEVER commit real keys
razorpay.keyId=${RAZORPAY_KEY_ID:rzp_test_changeMe}
razorpay.keySecret=${RAZORPAY_KEY_SECRET:changeMe}

# Google OAuth Configuration
# ===============================
google.clientId=${GOOGLE_CLIENT_ID:189236141996-658di7k0m2fuf4taqpn45uep5ivjgdi3.apps.googleusercontent.com}

# JWT Configuration
# ===============================
# Use environment variable for production - this should be a strong random secret
jwt.secret=${JWT_SECRET:a_very_long_and_secret_key_that_is_at_least_256_bits_long_for_hs256}

# Email Configuration
# ===============================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:creative.in47@gmail.com}
spring.mail.password=${MAIL_PASSWORD:qobk sbyj nkta awdg}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true